# Gutendex Loading Fix – v5.5.213

- Fixed the Library screen getting stuck on “Loading Library...” when the host or browser is slow/blocking external Gutendex requests.
- Local Faith In resources now render first without waiting for the external API.
- Gutendex books load in the background and merge into the same Library card UI.
- Added client-side request timeout and safe starter Project Gutenberg fallback cards so users do not see an endless spinner.
- Kept the same Library UI and categories.
