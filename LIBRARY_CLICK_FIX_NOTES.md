Faith In v5.5.187

Fixes:
- Restored the missing renderExplore() Library page renderer.
- Library menu clicks now open the real Library page instead of only highlighting the menu item.
- Library cards connect to selectResource() and the existing resource detail/download/save system.
- Search/category changes refresh the Library smoothly.
- Added safe Library render error fallback to avoid blank pages.
- Kept the existing WordPress REST resource API and upload system intact.
