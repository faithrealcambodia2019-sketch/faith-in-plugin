# Gutendex Visible Fix – v5.5.212

- Kept the existing Faith In Library UI.
- Made Gutendex / Project Gutenberg books appear directly in the Library first screen instead of only after local resources.
- Added a browser-side Gutendex fallback, so books can still load if the WordPress host blocks outgoing server requests.
- Kept local uploaded resources visible and working.
- API books open with **Open Book** and are not deletable.
- No Gutendex API key is required.
